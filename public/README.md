# Static assets

Place `gauntlet-model.mp4` here for the Gauntlet model video embed. The video will autoplay (muted) when users ask about Gauntlet's model in the AI chat.

Files in `public/` are served as static assets and do **not** increase the JavaScript bundle size.
